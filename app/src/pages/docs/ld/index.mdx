import DocsLayout from "../../../components/DocsLayout";

export const meta = {
    title: "Linked Data",
    author: "Jesse Hoogland"
}

# Linked Data



export default ({ children }) => (
  <DocsLayout {...meta}>
    {children}
  </DocsLayout>
)
